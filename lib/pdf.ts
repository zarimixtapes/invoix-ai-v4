'use client';import jsPDF from'jspdf';import html2canvas from'html2canvas';export async function exportInvoicePdf(filename='invoice.pdf'){const el=document.getElementById('invoice-print');if(!el)return alert('Invoice preview not found.');const canvas=await html2canvas(el,{scale:2,backgroundColor:'#ffffff'});const img=canvas.toDataURL('image/png');const pdf=new jsPDF('p','mm','a4');const w=210,h=(canvas.height*w)/canvas.width;pdf.addImage(img,'PNG',0,0,w,h);pdf.save(filename)}
