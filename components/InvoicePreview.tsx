'use client';import{useEffect,useState}from'react';import QRCode from'qrcode';import{Invoice,Template}from'@/lib/types';import{invoiceTotal}from'@/lib/share';export default function InvoicePreview({invoice,template}:{invoice:Invoice;template:Template}){const[qr,setQr]=useState('');useEffect(()=>{if(invoice.paymentQr)QRCode.toDataURL(invoice.paymentQr).then(setQr)},[invoice.paymentQr]);return <div id="invoice-print" className="rounded-[1.75rem] bg-white p-6 md:p-8 text-slate-900 shadow-2xl"><div className="flex flex-col gap-5 sm:flex-row sm:justify-between"><div className="flex gap-4"><div className="flex h-16 w-16 items-center justify-center overflow-hidden rounded-2xl text-xl font-black text-white" style={{background:template.accent}}>{invoice.business.logo?<img src={invoice.business.logo} className="h-full w-full object-cover"/>:template.logoText}</div><div><h2 className="text-3xl font-black">INVOICE</h2><p className="text-sm text-slate-500">#{invoice.number}</p></div></div><div className="text-sm sm:text-right"><p><b>Issue:</b> {invoice.issueDate}</p><p><b>Due:</b> {invoice.dueDate}</p><p className="mt-2 inline-block rounded-full px-3 py-1 text-white" style={{background:invoice.status==='Paid'?'#16a34a':template.accent}}>{invoice.status}</p></div></div><div className="mt-8 grid gap-4 rounded-2xl bg-slate-100 p-5 md:grid-cols-3"><div><p className="text-xs font-black uppercase text-slate-500">From</p><h3 className="mt-1 font-black">{invoice.business.name}</h3><p>{invoice.business.email}</p><p>{invoice.business.phone}</p><p>{invoice.business.address}</p>{invoice.business.abn&&<p>{invoice.business.businessIdLabel||'ABN'}: {invoice.business.abn}</p>}</div><div><p className="text-xs font-black uppercase text-slate-500">Bill To</p><h3 className="mt-1 font-black">{invoice.customer.name}</h3><p>{invoice.customer.phone}</p><p>{invoice.customer.email}</p><p>{invoice.customer.address}</p></div><div><p className="text-xs font-black uppercase text-slate-500">Payment</p><p>{invoice.business.bankName}</p><p>{invoice.business.accountName}</p><p>{invoice.business.bsb&&`BSB: ${invoice.business.bsb}`}</p><p>{invoice.business.accountNumber&&`ACC: ${invoice.business.accountNumber}`}</p><p>{invoice.business.payId&&`PayID: ${invoice.business.payId}`}</p></div></div><div className="mt-8 overflow-x-auto"><table className="w-full min-w-[720px] border-collapse text-sm"><thead><tr style={{background:template.accent}} className="text-white"><th className="rounded-l-xl p-3 text-left">SKU</th><th className="p-3 text-left">Description</th><th className="p-3 text-right">Qty</th><th className="p-3 text-right">Unit</th><th className="p-3 text-right">Rate</th><th className="p-3 text-right">Tax %</th><th className="p-3 text-right">Disc %</th><th className="rounded-r-xl p-3 text-right">Amount</th></tr></thead><tbody>{invoice.items.map(item=>{const sub=item.quantity*item.unitPrice;const disc=sub*((item.discount||0)/100);const total=(sub-disc)*(1+(item.tax||0)/100);return <tr key={item.id} className="border-b"><td className="p-3">{item.sku}</td><td className="p-3">{item.description}</td><td className="p-3 text-right">{item.quantity}</td><td className="p-3 text-right">{item.unit}</td><td className="p-3 text-right">${Number(item.unitPrice).toFixed(2)}</td><td className="p-3 text-right">{item.tax||0}</td><td className="p-3 text-right">{item.discount||0}</td><td className="p-3 text-right font-bold">${total.toFixed(2)}</td></tr>})}</tbody></table></div><div className="mt-8 flex flex-col items-end gap-4">{qr&&<img src={qr} className="h-28 w-28"/>}<div className="min-w-56 rounded-2xl p-5 text-right text-white" style={{background:template.accent}}><p className="text-sm opacity-80">Total Due</p><p className="text-3xl font-black">${invoiceTotal(invoice).toFixed(2)}</p></div></div>{invoice.business.signature&&<div className="mt-8"><p className="text-xs font-black uppercase text-slate-500">Signature</p><img src={invoice.business.signature} className="mt-2 max-h-20"/></div>}<p className="mt-8 text-sm text-slate-600">{invoice.notes||template.notes}</p></div>}
