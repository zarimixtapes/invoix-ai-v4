'use client';import AppShell from'@/components/AppShell';import Card from'@/components/Card';import InvoicePreview from'@/components/InvoicePreview';import{FREE_LIMITS,languages}from'@/lib/data';import{bumpUsage,getBusiness,getInvoices,getPlan,getTemplates,getUsage,saveInvoices}from'@/lib/local';import{exportInvoicePdf}from'@/lib/pdf';import{emailLink,gmailLink,outlookLink,smsLink,whatsappLink}from'@/lib/share';import{Invoice,Template}from'@/lib/types';import{Mic,Wand2}from'lucide-react';import{useEffect,useState}from'react';declare global{interface Window{webkitSpeechRecognition:any;SpeechRecognition:any}}export default function Ai(){const[text,setText]=useState('Create an invoice for Ahmed for garden cleaning, 3 hours, $180, due next Friday.'),[language,setLanguage]=useState('English'),[templates,setTemplates]=useState<Template[]>([]),[template,setTemplate]=useState<Template|null>(null),[invoice,setInvoice]=useState<Invoice|null>(null),[loading,setLoading]=useState(false),[listening,setListening]=useState(false);useEffect(()=>{const t=getTemplates();setTemplates(t);setTemplate(t[0])},[]);function voice(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR)return alert('Voice input needs Chrome or Edge.');const r=new SR();r.lang=language.includes('Arabic')?'ar-SA':'en-AU';r.onstart=()=>setListening(true);r.onend=()=>setListening(false);r.onresult=(e:any)=>setText(e.results[0][0].transcript);r.start()}async function generate(){const plan=getPlan(),usage=getUsage();if(plan==='free'&&usage.ai>=FREE_LIMITS.ai)return alert('Free AI limit reached. Upgrade.');setLoading(true);const res=await fetch('/api/ai-invoice',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text,language,currency:'AUD'})});const d=await res.json();const inv:Invoice={id:crypto.randomUUID(),number:`INV-${String(Date.now()).slice(-6)}`,customer:{id:crypto.randomUUID(),name:d.customerName||'New Customer',phone:d.customerPhone||'',email:d.customerEmail||'',address:d.customerAddress||''},business:getBusiness(),issueDate:new Date().toISOString().slice(0,10),dueDate:d.dueDateText||'7 days',status:'Draft',currency:d.currency||'AUD',language:d.invoiceLanguage||language,templateId:template?.id||'',items:(d.items||[]).map((i:any)=>({id:crypto.randomUUID(),sku:i.sku||'',description:i.description||'Service',quantity:Number(i.quantity||1),unit:i.unit||'job',unitPrice:Number(i.unitPrice||0),tax:Number(i.tax||0),discount:Number(i.discount||0)})),notes:d.notes||template?.notes||'',paymentQr:getBusiness().payId||''};setInvoice(inv);bumpUsage('ai');setLoading(false)}function save(){if(!invoice)return;saveInvoices([invoice,...getInvoices().filter(i=>i.id!==invoice.id)]);alert('Invoice saved.')}return <AppShell><div className='mb-6 rounded-[2rem] bg-gradient-to-r from-violet-700 to-blue-600 p-8 text-white'><h1 className='text-4xl font-black'>AI + Voice Invoice</h1><p>Prompt and voice map into the selected template preview.</p></div><div className='grid gap-6 xl:grid-cols-[460px_1fr]'><div className='space-y-4'><Card><select className='w-full rounded-2xl border p-3 text-black' value={language} onChange={e=>setLanguage(e.target.value)}>{languages.map(l=><option key={l.code} value={l.label}>{l.flag} {l.label}</option>)}</select><select className='mt-3 w-full rounded-2xl border p-3 text-black' value={template?.id||''} onChange={e=>setTemplate(templates.find(t=>t.id===e.target.value)||null)}>{templates.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select><textarea className='mt-3 min-h-48 w-full rounded-2xl border p-4 text-black' value={text} onChange={e=>setText(e.target.value)}/><div className='mt-4 grid grid-cols-2 gap-3'><button onClick={voice} className='rounded-2xl border py-3 font-black dark:border-line'><Mic className='inline'/> {listening?'Listening':'Voice'}</button><button onClick={generate} className='rounded-2xl bg-violet-600 py-3 font-black text-white'><Wand2 className='inline'/> {loading?'Generating':'Generate'}</button></div></Card></div><div>{invoice&&template?<InvoicePreview invoice={invoice} template={template}/>:<Card className='flex min-h-[520px] items-center justify-center text-center text-slate-500'>Generate an invoice to preview it here.</Card>}{invoice&&<div className='mt-4 flex flex-wrap gap-3'><button onClick={save} className='rounded-2xl bg-emerald-600 px-5 py-3 font-black text-white'>Save</button><button onClick={()=>exportInvoicePdf(invoice.number+'.pdf')} className='rounded-2xl border px-5 py-3 font-black dark:border-line'>PDF</button><a href={whatsappLink(invoice)} target='_blank' className='rounded-2xl bg-green-600 px-5 py-3 font-black text-white'>WhatsApp</a><a href={gmailLink(invoice)} target='_blank' className='rounded-2xl bg-red-600 px-5 py-3 font-black text-white'>Gmail</a><a href={outlookLink(invoice)} target='_blank' className='rounded-2xl bg-sky-700 px-5 py-3 font-black text-white'>Outlook</a><a href={emailLink(invoice)} className='rounded-2xl bg-blue-600 px-5 py-3 font-black text-white'>Email</a><a href={smsLink(invoice)} className='rounded-2xl bg-slate-800 px-5 py-3 font-black text-white'>SMS</a></div>}</div></div></AppShell>}
