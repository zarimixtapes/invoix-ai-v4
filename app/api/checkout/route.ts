import{NextResponse}from'next/server';import Stripe from'stripe';export async function POST(){const secret=process.env.STRIPE_SECRET_KEY,price=process.env.NEXT_PUBLIC_STRIPE_PRO_MONTHLY_PRICE_ID;if(!secret||!price)return NextResponse.json({error:'Stripe is not configured yet.'},{status:400});const stripe=new Stripe(secret);const s=await stripe.checkout.sessions.create({mode:'subscription',line_items:[{price,quantity:1}],success_url:`${process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000'}/dashboard?success=true`,cancel_url:`${process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000'}/pricing?cancelled=true`});return NextResponse.json({url:s.url})}
