import{NextResponse}from'next/server';import Stripe from'stripe';export async function POST(req:Request){const secret=process.env.STRIPE_SECRET_KEY;if(!secret)return NextResponse.json({error:'Stripe is not configured yet.'},{status:400});const{customerId}=await req.json().catch(()=>({customerId:''}));if(!customerId)return NextResponse.json({error:'No Stripe customer ID yet. Production webhooks save this automatically after checkout.'},{status:400});const stripe=new Stripe(secret);const s=await stripe.billingPortal.sessions.create({customer:customerId,return_url:`${process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000'}/pricing`});return NextResponse.json({url:s.url})}
