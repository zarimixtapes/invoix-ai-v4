'use client';import{useState}from'react';import{supabase,hasSupabase}from'@/lib/supabase';import Link from'next/link';export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');async function oauth(provider:'google'|'facebook'|'github'){if(!hasSupabase)return setMsg('Add Supabase keys first.');await supabase.auth.signInWithOAuth({provider,options:{redirectTo:location.origin+'/dashboard'}})}async function signup(){if(!hasSupabase)return setMsg('Add Supabase keys first.');const{error}=await supabase.auth.signUp({email,password});setMsg(error?error.message:'Signup successful. Check email if confirmation is enabled.')}async function login(){if(!hasSupabase)return setMsg('Add Supabase keys first.');const{error}=await supabase.auth.signInWithPassword({email,password});if(error)setMsg(error.message);else location.href='/dashboard'}return <main className='min-h-screen bg-ink p-6 text-white'><div className='mx-auto mt-16 max-w-md rounded-3xl border border-line bg-card p-8'><Link href='/' className='text-3xl font-black'>Invoix<span className='text-violet-500'>AI</span></Link><h1 className='mt-8 text-3xl font-black'>Login or create account</h1><div className='mt-6 grid gap-3'><button onClick={()=>oauth('google')} className='rounded-2xl bg-white py-3 font-black text-slate-900'>Continue with Google</button><button onClick={()=>oauth('facebook')} className='rounded-2xl bg-blue-600 py-3 font-black'>Continue with Facebook</button><button onClick={()=>oauth('github')} className='rounded-2xl bg-slate-800 py-3 font-black'>Continue with GitHub</button></div><input className='mt-6 w-full rounded-2xl border border-line bg-card2 p-4 text-white' placeholder='Email' value={email} onChange={e=>setEmail(e.target.value)}/><input className='mt-3 w-full rounded-2xl border border-line bg-card2 p-4 text-white' placeholder='Password' type='password' value={password} onChange={e=>setPassword(e.target.value)}/><button onClick={login} className='mt-5 w-full rounded-2xl bg-violet-600 py-4 font-black'>Login</button><button onClick={signup} className='mt-3 w-full rounded-2xl border border-line py-4 font-black'>Create account</button>{msg&&<p className='mt-4 text-sm text-yellow-300'>{msg}</p>}</div></main>}
